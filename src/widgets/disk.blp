using Gtk 4.0;
using Adw 1;

template DiskEntry : Adw.ActionRow {
    title: "Disk";
    subtitle: "disk size";
    Gtk.Box {
        Gtk.CheckButton select_button {
            use-underline: true;
        }
    }
}